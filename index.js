let string = "Julio"; //Textos.
let number = 1234; //Inteiros e decimais.
let boolean = true; //Verdadeiro ou falso.
let undefined; //Variavel declarada, mas sem valor.
let indefinido = null; //Ausencia de valor.

console.log("Hello Word");

if (num1 == 5 && num2 == 5) {
    //verdadeiro
}

if (num1 == 5 || num2 == 5) {
    //verdadeiro
}

const ativo = false;
if (!ativo) {
    console.log("O usuário não stá ativo.")
}
